pre-commit:
  parallel: true
  jobs:
    - name: fmt
      glob: '*.{ts,tsx,js,jsx,css,md,json,yml,yaml}'
      run: bunx oxfmt {staged_files}
      stage_fixed: true

    - name: lint
      glob: '*.{mts,ts,tsx,js,jsx,mjs}'
      run: bunx oxlint --fix {staged_files}
      stage_fixed: true
